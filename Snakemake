

rule all_models:
    input:
        "api/model_pickles/logreg.pickle"

rule build_model:
    input:
        datadir="data/"
        "data/geoplaces2.csv",
        "data/chefmozparking.csv",
        "data/rating_final.csv",
    output:
        "api/model_pickles/{modelname}.pickle"
    shell:
        "jupyter nbconvert --to notebook models"